# プロジェクト同梱の環境変数（ビルド時に焼き込まれる）。
# Vercel を使わなくてもこのファイルだけで設定が完結します。
# 秘密情報は .env.local（gitignore 済み）へ。

# 本文の表示切り替え
#   true（既定） → 発文者・本文・画像を表示
#   false        → 画像のみモード（本文を隠し、絵師クレジットだけ表示）
VITE_SHOW_TWEET_TEXT=true
