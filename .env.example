# 環境変数のテンプレート。実際の値は .env（同梱・コミット済み）に書きます。
# 秘密情報を足す場合は .env.local（gitignore 済み）に。
#
# Vite の仕様上、ブラウザに露出してよい値のみ VITE_ 接頭辞を付けます。

# 本文の表示切り替え
#   true（既定） → 発文者・本文・画像を表示
#   false        → 画像のみモード（本文を隠し、絵師クレジットだけ表示）
VITE_SHOW_TWEET_TEXT=true
